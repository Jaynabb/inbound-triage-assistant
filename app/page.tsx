// Placeholder — the real UI is built in Phase 3.
// This exists so `next dev` has something to serve during the boot check.
export default function Page() {
  return <main>Inbound Triage Assistant</main>;
}
