node_modules/
.next/
out/
build/

# Secrets — never commit. .env.example is the committed template.
.env
.env.local
.env*.local

# Run output is regenerated by scripts/eval.mts and scripts/run-triage.mts,
# which both create the directory on demand. Nothing in here is a deliverable.
outputs/

.DS_Store
*.log

# Working notes — folded into RATIONALE.md, not a deliverable
NOTES.md

# build artifact
tsconfig.tsbuildinfo
